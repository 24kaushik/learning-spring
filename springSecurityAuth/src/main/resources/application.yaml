spring:
  application:
    name: springSecurityAuth

  security:
    user:
      name: kaushik
      password: kaushik

  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: kaushik

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true