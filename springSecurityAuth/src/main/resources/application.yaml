spring:
  application:
    name: springSecurityAuth

  security:
    user:
      name: kaushik
      password: kaushik

  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: kaushik

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

jwt.secret: f//HIlwRu2A3N20rvZ/EGujbnryXyOgLT/QJPvixHIA=
jwt.issuer: springSecurityAuth api
jwt.expiry: 900